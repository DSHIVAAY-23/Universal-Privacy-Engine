# Notary Service Environment Variables
# Copy this to .env and fill in real values. Never commit .env.

# Notary private key — secp256k1, used to sign STLOP proofs.
# Any valid Ethereum private key works for local dev.
NOTARY_PRIVATE_KEY=0x0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Server port (default: 3002)
PORT=3002

# Log level: trace | debug | info | warn | error
RUST_LOG=info

# Set to 1 for full backtraces on panic (useful during dev)
RUST_BACKTRACE=0
