# Notary Service Environment Variables

# CRITICAL: Never commit this file with real values!
# This is the private key used to sign STLOP proofs

# Notary Private Key (ECDSA secp256k1)
# For testing, use the same key as frontend mock proofs
NOTARY_PRIVATE_KEY=0x0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Server Port (default: 3001)
PORT=3001

# Log Level (trace, debug, info, warn, error)
RUST_LOG=info
